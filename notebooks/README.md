# Notebooks

Keep training code in `src/skip_vae_rl`.

Use notebooks only for:

```text
1. comparing reconstructions
2. plotting learning curves
3. preparing final report figures
4. inspecting latent embeddings
```

